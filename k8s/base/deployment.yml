apiVersion: apps/v1
kind: Deployment
metadata:
  name: comin-authentication-service
  namespace: comin-dev
  labels:
    app: comin-authentication-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: comin-authentication-service
  template:
    metadata:
      labels:
        app: comin-authentication-service
    spec:
      containers:
      - name: comin-authentication-service
        image: ghcr.io/kaveeshag/comin-authentication-service:dev
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: GO_ENV
          value: "development"
        resources:
          limits:
            cpu: "0.5"
            memory: "512Mi"
          requests:
            cpu: "0.2"
            memory: "256Mi"